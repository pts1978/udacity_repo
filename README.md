# Udacity_Machine_Learning_DevOps_Engineer
Files related to the Udacity Nanodegree "Machine Learning DevOps Engineer"

bei github anmelden!
git config --global user.name "Your Name"
git config --global user.email "your_email@example.com"

repo auf den lokalen Rechner runter laden \n 
git clone "url to the repo"

Datei hinzufügen
git add

Änderung bestätigen und kommentieren
git commit -m "README updated"

Änderung ins repo hochladen
git push
